<?php
if ($argc > 1) {
    switch ($argv[1]) {
        case 'test':
            passthru('php phpunit tests --color --display-incomplete --testdox');
            break;
        default:
            # code...
            break;
    }
}